local snacks = require("snacks")

local config = {
    input = {
        icon = "",
        prompt_pos = "left",
    },

    styles = {
        input = {
            position = "bottom"
        },
    }
}

snacks.setup(config)
